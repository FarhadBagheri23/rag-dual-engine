# grows one phase at a time, see README
# --- phase 0 ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
python-multipart==0.0.20
# --- phase 1: parsing & chunking ---
pymupdf==1.25.2
python-docx==1.1.2
langchain-text-splitters==0.3.5
# --- phase 4: embeddings, vector store, generation ---
# Install CPU-only torch FIRST — the default PyPI wheel bundles ~2.5 GB of CUDA
# kernels this project never uses (all-MiniLM-L6-v2 runs on CPU by design):
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.6.0
torch==2.6.0
chromadb==0.6.3
sentence-transformers==3.4.1
openai==1.60.2
